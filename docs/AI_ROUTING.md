# AI Routing — Version 1.2.0

Smart mode uses the Claude adapter for technical work.

Chat mode uses the OpenAI adapter for general assistance.

Language mode uses the Gemini adapter for translation/localization tasks.

Auto mode performs deterministic lightweight classification before selecting a provider. It does not make an extra model call solely to classify a request.
