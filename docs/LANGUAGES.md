# Languages — Version 1.2.0

The product currently exposes exactly nine locale codes:

`km`, `en`, `zh`, `vi`, `ja`, `ko`, `id`, `fil`, `ru`.

To add another locale safely, update the registry, add the locale resource, add tests, and update this document.
